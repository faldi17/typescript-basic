"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        let name = "Ahmad Ali";
        let balance = 150000;
        let isVip = false;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
