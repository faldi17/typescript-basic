describe('Data Type', function () {
    it('should must declare', function () {
        const name: string = "Ahmad Ali";
        const balance: number = 150000;
        const isVip: boolean = false;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
})
